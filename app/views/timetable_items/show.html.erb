<% content_for :title, 'Timetable Item' %>
<div class="alert alert-primary" role="alert">
  <p class="mb-0"><%= link_to 'Edit', edit_timetable_item_path(@timetable_item) if can? :update, @timetable_item %></p>
  <p class="mb-0"><%= link_to 'Destroy', timetable_item_path(@timetable_item), method: :delete, data: {confirm: 'Are you sure?'} if can? :destroy, @timetable_item %></p>
</div>
<p><%= @timetable_item.when %></p>
<p><%= @timetable_item.description %></p>
<div class="fixed-bottom bg-white">
  <p class="container"><%= link_to '⇦ Back to Timetable', timetable_items_path %></p>
</div>
